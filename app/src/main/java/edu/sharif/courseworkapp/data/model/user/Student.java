package edu.sharif.courseworkapp.data.model.user;

public class Student extends User {
    public Student(String userId, String firstname, String lastname, String password) {
        super(userId, firstname, lastname, password);
    }
}
